apiVersion: req.nephio.org/v1alpha1
kind: Interface
metadata:
  name: sbi
  annotations:
    config.kubernetes.io/local-config: "true"
    specializer.nephio.org/owner: workload.nephio.org/v1alpha1.UDMDeployment.udm-example
    specializer.nephio.org/namespace: oai-cp
spec:
  networkInstance:
    name: default
